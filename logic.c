
#include<stdio.h>

int main()
{

    // Logical operator

    //Logical AND (&&)

    // Logical OR (||)

    // Logical NOT(!)

    // Logical AND is used to check more than one condition. It will return true if all the
    // conditions will be true


    int a=10,b=20,c=5;

    if(a>b && a>c)

    {

        printf("a is greatest");
    }

    else if(b>a && b>c)

    {

        printf("b is greatest");

    }

    else{

        printf(" c is greatest");
    }

    return 0;
}
