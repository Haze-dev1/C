#include<stdio.h>
int main()
{
int b,c;
char a[10];
char a[10];

printf("What is your name:");
scanf("%s",&a);

printf("What is your age:");
scanf("%d",&b);

printf("So your name is %s and your age is %d",a,b);

return 0;
}
