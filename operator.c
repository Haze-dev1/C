#include<stdio.h>

int main()
{

    // operator

    //Arithmetic operator

    //+,-,*,/,%,++,--

    //% is called as modulo operator and it will always returns an remainder

    // it will not work with floating point values

    int a=5,b=2,c;
    a+=b; //a=a+1; //++ increment operator

   // a--;  //-- is called as decrement operator

    printf("%d",a+=b);

    return 0;
}
